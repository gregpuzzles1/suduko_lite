# Contracts (No Network API)

This feature is a static web app with no backend/network API.

The “contracts” here document:
- Browser persistence formats (if any)
- Internal module boundaries that should remain stable
